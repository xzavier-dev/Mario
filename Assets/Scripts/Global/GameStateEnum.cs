public enum CameraView_t{
    CV2D,
    CV3D,
}


public enum PlayerState_t {
    WALKING,
    JUMPDOWN,
    JUMPUP,
    GROUNDED,
}


public enum PlayerSize_t {
    NORMAL,
    BIG,
    FIRE,
}

public enum CollisionFlag_t {
    HEAD,
    FOOT,
    BODY,
}
